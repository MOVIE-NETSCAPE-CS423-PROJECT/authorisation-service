server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/netscape_auth_db
    username: postgres
    password: Benklins@123
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect


  data:
    redis:
      host: localhost
      port: 6379


